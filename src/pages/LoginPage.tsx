import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <section className="page-card">
      <h1>Login</h1>
      <LoginForm />
    </section>
  );
};

export default LoginPage;
